<VirtualHost *:443>
    ServerName ip87-106-3-165.pbiaas.com

    # Chemin vers le dossier du site WordPress
    DocumentRoot /var/www/html/wordpress

    # Certificats auto-signés (Certbot remplacera ces lignes automatiquement)
    SSLEngine On
    SSLCertificateFile /etc/ssl/certs/ssl-cert-snakeoil.pem
    SSLCertificateKeyFile /etc/ssl/private/ssl-cert-snakeoil.key

    <Directory /var/www/html/wordpress>
        AllowOverride All
        Options -Indexes
        Require all granted
    </Directory>

    # Configuration des entêtes CORS (nécessaire pour autoriser le front React à appeler l'API WooCommerce)
    <IfModule mod_headers.c>
        Header set Access-Control-Allow-Origin "*"
        Header set Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS"
        Header set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With"
    </IfModule>

    # Gestion des en-têtes HTTPS en cas d'utilisation d'un reverse proxy
    SetEnvIf X-Forwarded-Proto https HTTPS=on
</VirtualHost>
